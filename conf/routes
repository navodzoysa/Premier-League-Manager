# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Serve index page from public directory
GET     /                           controllers.FrontendController.index()

# APIs in SportsClubController
GET     /api/sportsclub             controllers.SportsClubController.listSportsClubs()

# APIs in MatchController
GET     /api/match                  controllers.MatchController.listMatches()
+nocsrf
POST    /api/match                  controllers.MatchController.create()

# An example route (Prefix all API routes with apiPrefix defined in application.conf)
GET     /api/summary                controllers.HomeController.appSummary

# Serve static assets under public directory
GET     /*file                      controllers.FrontendController.assetOrDefault(file)

# Test post request
POST    /api/postTest               controllers.HomeController.postTest()
